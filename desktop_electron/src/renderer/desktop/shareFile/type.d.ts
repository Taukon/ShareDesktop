export type FileInfo = {
  fileTransferId: string;
  fileName: string;
  fileSize: number;
};

export type WriteFile = {
  // fileTransferId: string;
  fileName: string;
  fileSize: number;
};

export type ReadFile = {
  // fileTransferId: string;
  fileName: string;
};
